  datasource:
    url: jdbc:h2:mem:loansdb
    username: sa
    password: ''
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace

build:
  name: Loans Service
  version: d1
  profile: DEFAULT